export interface BadgeDetail {
  id: number;
  badgeId: string;
  badgeUrl: string;
  badgeImageUrl: string;
  created: string;
  issuerUrl: string;
  image: string;
  name: string;
  description: string;
}
